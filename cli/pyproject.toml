[project]
name = "claws-cli"
version = "0.1.0"
description = "Meta-CLI for Lobster Claws skill discovery and routing"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
claws = "claws_cli.main:main"
