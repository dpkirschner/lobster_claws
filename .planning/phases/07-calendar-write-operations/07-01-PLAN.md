---
phase: 07-calendar-write-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/calendar/src/claws_calendar/calendar.py
  - skills/calendar/tests/test_calendar_api.py
autonomous: true
requirements: [CAL-03, CAL-04, CAL-05]

must_haves:
  truths:
    - "create_event() POSTs to Calendar API and returns formatted event detail"
    - "update_event() PUTs to Calendar API with partial body and returns formatted event detail"
    - "delete_event() DELETEs from Calendar API and returns confirmation dict"
    - "All three functions acquire token via get_access_token()"
    - "HTTP helpers _calendar_post, _calendar_put, _calendar_delete follow _calendar_get pattern"
  artifacts:
    - path: "skills/calendar/src/claws_calendar/calendar.py"
      provides: "HTTP helpers and business logic for create, update, delete"
      exports: ["_calendar_post", "_calendar_put", "_calendar_delete", "create_event", "update_event", "delete_event"]
    - path: "skills/calendar/tests/test_calendar_api.py"
      provides: "Tests for all three write operations"
      contains: "test_create_event"
  key_links:
    - from: "create_event"
      to: "_calendar_post"
      via: "POST /events"
      pattern: "_calendar_post.*events"
    - from: "update_event"
      to: "_calendar_put"
      via: "PUT /events/{event_id}"
      pattern: "_calendar_put.*events/"
    - from: "delete_event"
      to: "_calendar_delete"
      via: "DELETE /events/{event_id}"
      pattern: "_calendar_delete.*events/"
---

<objective>
Add write-operation functions to the Calendar API module: HTTP helpers (_calendar_post, _calendar_put, _calendar_delete) and business logic (create_event, update_event, delete_event), with full test coverage.

Purpose: Provides the API layer that CLI subcommands (Plan 02) will call for calendar write operations.
Output: Updated calendar.py with 6 new functions, updated test_calendar_api.py with tests for all three operations.
</objective>

<execution_context>
@/Users/little-dank/.claude/get-shit-done/workflows/execute-plan.md
@/Users/little-dank/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/calendar/src/claws_calendar/calendar.py
@skills/calendar/tests/test_calendar_api.py

<interfaces>
<!-- Existing functions in calendar.py that new code will reuse -->

```python
# Already defined — reuse directly
CALENDAR_BASE = "https://www.googleapis.com/calendar/v3/calendars/primary"

def get_access_token() -> str: ...
def _calendar_get(path: str, token: str, params: dict | None = None) -> dict: ...
def format_event_detail(event: dict) -> dict: ...
def handle_calendar_error(e: httpx.HTTPStatusError) -> None: ...

# From claws_common.output (already imported: crash, fail)
def result(data): ...   # Not imported yet in calendar.py — only needed in cli.py
```

<!-- Test fixtures already defined in test_calendar_api.py -->
```python
@pytest.fixture
def mock_auth_client(): ...   # Patches ClawsClient, returns test-token
@pytest.fixture
def mock_httpx(): ...         # Patches httpx in calendar module
@pytest.fixture
def sample_timed_event(): ... # Full event dict with all fields
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add HTTP helpers and write functions to calendar.py</name>
  <files>skills/calendar/src/claws_calendar/calendar.py, skills/calendar/tests/test_calendar_api.py</files>
  <read_first>
    - skills/calendar/src/claws_calendar/calendar.py
    - skills/calendar/tests/test_calendar_api.py
    - skills/gmail/src/claws_gmail/gmail.py (reference for _gmail_post pattern)
  </read_first>
  <behavior>
    - test_calendar_post: _calendar_post calls httpx.post with CALENDAR_BASE+path, json=body, Authorization header, timeout=30.0, returns resp.json()
    - test_calendar_put: _calendar_put calls httpx.put with same pattern as _calendar_post
    - test_calendar_delete: _calendar_delete calls httpx.delete with CALENDAR_BASE+path, Authorization header, timeout=30.0, calls raise_for_status (no return value needed)
    - test_create_event: create_event calls get_access_token, builds body from args, calls _calendar_post("/events", ...), returns format_event_detail(response)
    - test_create_event_minimal: create_event with only title+start+end builds correct body with summary, start.dateTime, end.dateTime
    - test_create_event_full: create_event with title+start+end+location+description+attendees includes all fields in body
    - test_create_event_all_day: create_event with all_day=True uses start.date and end.date instead of dateTime
    - test_update_event: update_event calls get_access_token, builds body from only non-None kwargs, calls _calendar_put("/events/{event_id}", ...), returns format_event_detail(response)
    - test_update_event_partial: update_event with only title set sends body with only {"summary": "new title"}
    - test_delete_event: delete_event calls get_access_token, calls _calendar_delete("/events/{event_id}", ...), returns {"deleted": True, "event_id": event_id}
  </behavior>
  <action>
    Write tests first in test_calendar_api.py, then implement in calendar.py.

    **HTTP Helpers** — add after _calendar_get, following same pattern:

    1. `_calendar_post(path: str, token: str, body: dict) -> dict` — httpx.post to CALENDAR_BASE+path, json=body, Authorization Bearer header, timeout=30.0, raise_for_status(), return resp.json()

    2. `_calendar_put(path: str, token: str, body: dict) -> dict` — same as post but httpx.put

    3. `_calendar_delete(path: str, token: str) -> None` — httpx.delete to CALENDAR_BASE+path, Authorization Bearer header, timeout=30.0, raise_for_status(), no return (204 No Content)

    **Business Logic** — add after get_event:

    4. `create_event(title: str, start: str, end: str, *, location: str | None = None, description: str | None = None, attendees: list[str] | None = None, all_day: bool = False) -> dict`:
       - Calls get_access_token()
       - Builds body: {"summary": title}
       - If all_day: body["start"] = {"date": start}, body["end"] = {"date": end}
       - Else: body["start"] = {"dateTime": start}, body["end"] = {"dateTime": end}
       - If location: body["location"] = location
       - If description: body["description"] = description
       - If attendees: body["attendees"] = [{"email": e} for e in attendees]
       - Calls _calendar_post("/events", token, body)
       - Returns format_event_detail(response)

    5. `update_event(event_id: str, *, title: str | None = None, start: str | None = None, end: str | None = None, location: str | None = None, description: str | None = None, attendees: list[str] | None = None) -> dict`:
       - Calls get_access_token()
       - Builds body from only non-None kwargs: title -> "summary", start -> "start": {"dateTime": start}, end -> "end": {"dateTime": end}, location -> "location", description -> "description", attendees -> "attendees": [{"email": e} for e in attendees]
       - Calls _calendar_put(f"/events/{event_id}", token, body)
       - Returns format_event_detail(response)

    6. `delete_event(event_id: str) -> dict`:
       - Calls get_access_token()
       - Calls _calendar_delete(f"/events/{event_id}", token)
       - Returns {"deleted": True, "event_id": event_id}
  </action>
  <verify>
    <automated>cd /Users/little-dank/code/lobster_claws && uv run pytest skills/calendar/tests/test_calendar_api.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def _calendar_post" skills/calendar/src/claws_calendar/calendar.py
    - grep -q "def _calendar_put" skills/calendar/src/claws_calendar/calendar.py
    - grep -q "def _calendar_delete" skills/calendar/src/claws_calendar/calendar.py
    - grep -q "def create_event" skills/calendar/src/claws_calendar/calendar.py
    - grep -q "def update_event" skills/calendar/src/claws_calendar/calendar.py
    - grep -q "def delete_event" skills/calendar/src/claws_calendar/calendar.py
    - grep -q "test_create_event" skills/calendar/tests/test_calendar_api.py
    - grep -q "test_update_event" skills/calendar/tests/test_calendar_api.py
    - grep -q "test_delete_event" skills/calendar/tests/test_calendar_api.py
    - uv run pytest skills/calendar/tests/test_calendar_api.py -x passes with 0 failures
  </acceptance_criteria>
  <done>
    All six new functions exist in calendar.py. Tests for create_event (minimal, full, all-day), update_event (full, partial), and delete_event pass. Existing tests still pass.
  </done>
</task>

</tasks>

<verification>
- All existing calendar API tests still pass
- New tests cover create, update, delete with edge cases
- `uv run pytest skills/calendar/tests/test_calendar_api.py -x -v` passes
</verification>

<success_criteria>
- calendar.py exports 6 new functions: _calendar_post, _calendar_put, _calendar_delete, create_event, update_event, delete_event
- test_calendar_api.py has at least 10 new test functions covering write operations
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-calendar-write-operations/07-01-SUMMARY.md`
</output>
