---
phase: 03-discovery-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [INFR-04]

must_haves:
  truths:
    - "README documents the monorepo structure with all directories explained"
    - "README shows how to install skills in the OpenClaw container"
    - "README shows how to set up and manage the whisper server"
    - "README explains how to add a new skill step by step"
  artifacts:
    - path: "README.md"
      provides: "Project README with repo structure, installation, server setup, and new skill guide"
      min_lines: 80
  key_links: []
---

<objective>
Create the project README documenting repo structure, skill installation, server setup, and how to add a new skill.

Purpose: New contributors can understand the project and add new skills without reading source code. This is the project's front door.

Output: Top-level README.md with comprehensive documentation.
</objective>

<execution_context>
@/Users/little-dank/.claude/get-shit-done/workflows/execute-plan.md
@/Users/little-dank/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write project README</name>
  <files>README.md</files>
  <read_first>
    - .planning/PROJECT.md
    - data.md
    - pyproject.toml
    - common/pyproject.toml
    - skills/transcribe/pyproject.toml
    - skills/transcribe/src/claws_transcribe/cli.py
    - common/src/claws_common/__init__.py
  </read_first>
  <action>
    Create README.md with these sections:

    1. **Title and tagline**: "Lobster Claws" -- AI agent skills as thin container CLIs paired with macOS host servers. Brief 2-sentence description of what this is and the core pattern (CLI in container -> HTTP to host server -> stdout result).

    2. **Architecture Overview**: Explain the two-layer design:
       - Skills (CLIs) run inside the OpenClaw Docker container (node:24-bookworm)
       - Servers run on the Mac mini host (macOS, Apple Silicon)
       - Communication via host.docker.internal / OPENCLAW_TOOLS_HOST
       - Include a simple ASCII diagram showing: Container [claws-transcribe] --HTTP--> Host [whisper-server]

    3. **Repo Structure**: Tree showing:
       ```
       lobster_claws/
         common/          # claws-common: shared client library (host resolution, HTTP, output)
         cli/             # claws-cli: meta-CLI for skill discovery and routing
         skills/
           transcribe/    # claws-transcribe: audio transcription via whisper
         servers/
           whisper/       # whisper-server: FastAPI + mlx-whisper on Apple Silicon
         pyproject.toml   # uv workspace root
       ```

    4. **Quick Start / Installation**:
       - Prerequisites: Python 3.12+, uv
       - Dev setup: `git clone`, `uv sync`, `uv run claws` to verify
       - Container installation: `pip install --break-system-packages git+<repo>#subdirectory=common git+<repo>#subdirectory=skills/transcribe`
       - Note about OPENCLAW_TOOLS_HOST env var for host resolution

    5. **Available Skills**:
       - transcribe: `claws transcribe <audio-file> [--model MODEL] [--format text|json]`
       - Brief description of what it does and which server it talks to

    6. **Server Setup**:
       - Whisper server: port 8300, requires mlx-whisper
       - Starting manually: `uv run uvicorn whisper_server.app:app --host 0.0.0.0 --port 8300`
       - launchd auto-start: reference the plist at servers/whisper/com.lobsterclaws.whisper-server.plist
       - Health check: `curl http://localhost:8300/health`

    7. **Adding a New Skill** (step-by-step guide):
       a. Create skill directory: `skills/<name>/`
       b. Create pyproject.toml with claws-common dependency, [project.scripts] entry, and [project.entry-points."claws.skills"] registration
       c. Create src/claws_<name>/cli.py following the pattern: argparse + ClawsClient + result/fail/crash
       d. Create corresponding server if needed in servers/<name>/
       e. Add to root pyproject.toml dev deps and uv.sources
       f. Run `uv sync`
       g. Verify with `claws` (should list new skill)

    8. **Environment Variables**:
       Table of OPENCLAW_TOOLS_HOST, WHISPER_SERVER_PORT, WHISPER_MODEL with defaults and descriptions

    9. **Development**:
       - Running tests: `uv run pytest`
       - Linting: `uv run ruff check .`
       - Adding workspace members: explain the uv workspace pattern

    Keep the tone direct and practical. No marketing language. Use the lobster/claw metaphor sparingly (title and "adding a new claw" section only).
  </action>
  <verify>
    <automated>cd /Users/little-dank/code/lobster_claws && test -f README.md && wc -l README.md | awk '{if ($1 >= 80) print "PASS: README has sufficient content (" $1 " lines)"; else print "FAIL: README too short (" $1 " lines)"}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` succeeds (file exists)
    - `grep -q "Repo Structure\|Repository Structure\|Repo structure" README.md` succeeds (has structure section)
    - `grep -q "pip install.*break-system-packages" README.md` succeeds (has container install instructions)
    - `grep -q "launchd\|plist" README.md` succeeds (has server setup with launchd)
    - `grep -q "Adding a New Skill\|Adding a new skill\|Add a New Skill" README.md` succeeds (has new skill guide)
    - `grep -q "claws.skills" README.md` succeeds (documents entry point registration)
    - `grep -q "OPENCLAW_TOOLS_HOST" README.md` succeeds (documents host env var)
    - `wc -l README.md` shows at least 80 lines
  </acceptance_criteria>
  <done>
    - README.md exists at repo root with all required sections
    - Documents repo structure, installation, server setup, and how to add a new skill
    - References the entry point discovery mechanism for skill registration
  </done>
</task>

</tasks>

<verification>
- README.md exists and covers all four areas: repo structure, skill installation, server setup, adding new skills
- All grep checks in acceptance_criteria pass
</verification>

<success_criteria>
INFR-04 satisfied: Top-level README covers repo structure, skill installation, server setup, and how to add new skills.
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-and-documentation/03-02-SUMMARY.md`
</output>
