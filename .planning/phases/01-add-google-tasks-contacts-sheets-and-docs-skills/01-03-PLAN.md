---
phase: 01-add-google-tasks-contacts-sheets-and-docs-skills
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sheets/pyproject.toml
  - skills/sheets/src/claws_sheets/__init__.py
  - skills/sheets/src/claws_sheets/sheets.py
  - skills/sheets/src/claws_sheets/cli.py
  - skills/sheets/tests/test_sheets.py
  - skills/sheets/tests/test_sheets_cli.py
  - pyproject.toml
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-13
  - D-14
  - D-15
  - D-16
  - D-17

must_haves:
  truths:
    - "claws sheets list shows spreadsheets via Drive API"
    - "claws sheets read SPREADSHEET_ID RANGE returns cell values"
    - "claws sheets write SPREADSHEET_ID RANGE --values '[[...]]' writes cell values"
    - "claws sheets create --title NAME creates a new spreadsheet"
    - "--as flag delegates identity"
  artifacts:
    - path: "skills/sheets/src/claws_sheets/sheets.py"
      provides: "Sheets API client"
      exports: ["get_access_token", "list_spreadsheets", "read_values", "write_values", "create_spreadsheet", "handle_sheets_error"]
    - path: "skills/sheets/src/claws_sheets/cli.py"
      provides: "Sheets CLI entry point"
      exports: ["main"]
    - path: "skills/sheets/pyproject.toml"
      provides: "Package config with entry point"
      contains: "claws.skills"
  key_links:
    - from: "skills/sheets/src/claws_sheets/cli.py"
      to: "skills/sheets/src/claws_sheets/sheets.py"
      via: "import"
      pattern: "from claws_sheets.sheets import"
    - from: "skills/sheets/src/claws_sheets/sheets.py"
      to: "claws_common.client"
      via: "ClawsClient for auth"
      pattern: "ClawsClient.*google-auth.*8301"
---

<objective>
Add the Google Sheets skill (claws-sheets) with data-only read/write operations using A1 notation.

Purpose: Enable the OpenClaw agent to list, create, read, and write spreadsheet cell values. No formatting, charts, or formulas -- data only (per D-07).
Output: Working claws-sheets package with API client, CLI, tests, and workspace registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-add-google-tasks-contacts-sheets-and-docs-skills/01-CONTEXT.md
@.planning/phases/01-add-google-tasks-contacts-sheets-and-docs-skills/01-RESEARCH.md

<interfaces>
From common/src/claws_common/client.py:
```python
class ClawsClient:
    def __init__(self, service: str, port: int): ...
    def post_json(self, path: str, body: dict) -> dict: ...
```

From common/src/claws_common/output.py:
```python
def result(data: dict | list | str) -> None: ...
def fail(message: str) -> None: ...
def crash(message: str) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Sheets API client module with tests</name>
  <files>
    skills/sheets/pyproject.toml,
    skills/sheets/src/claws_sheets/__init__.py,
    skills/sheets/src/claws_sheets/sheets.py,
    skills/sheets/tests/test_sheets.py
  </files>
  <read_first>
    skills/gmail/src/claws_gmail/gmail.py,
    skills/gmail/tests/test_gmail.py,
    skills/drive/src/claws_drive/drive.py,
    skills/gmail/pyproject.toml,
    common/src/claws_common/client.py,
    common/src/claws_common/output.py
  </read_first>
  <behavior>
    - get_access_token() requests BOTH scopes: ["https://www.googleapis.com/auth/spreadsheets", "https://www.googleapis.com/auth/drive.readonly"] (per D-09, dual scope for list)
    - get_access_token(as_user="alice@x.com") includes "subject" in POST body
    - list_spreadsheets(max_results=100) calls Drive API GET https://www.googleapis.com/drive/v3/files with q=mimeType='application/vnd.google-apps.spreadsheet', returns file list
    - read_values(spreadsheet_id, range_) calls GET https://sheets.googleapis.com/v4/spreadsheets/{id}/values/{range}, returns values 2D array
    - write_values(spreadsheet_id, range_, values) calls PUT .../values/{range}?valueInputOption=USER_ENTERED with body {"range": range_, "values": values}
    - create_spreadsheet(title) calls POST https://sheets.googleapis.com/v4/spreadsheets with {"properties": {"title": title}}
    - handle_sheets_error: 401->crash, 403->fail, 404->fail, 429->fail, other->crash
  </behavior>
  <action>
    1. Create `skills/sheets/pyproject.toml` (per D-16, D-17):
       ```toml
       [project]
       name = "claws-sheets"
       version = "0.1.0"
       description = "Google Sheets skill for Lobster Claws"
       requires-python = ">=3.11"
       dependencies = ["claws-common"]

       [build-system]
       requires = ["hatchling"]
       build-backend = "hatchling.build"

       [project.entry-points."claws.skills"]
       sheets = "claws_sheets.cli:main"

       [tool.uv.sources]
       claws-common = { workspace = true }
       ```

    2. Create empty `skills/sheets/src/claws_sheets/__init__.py`

    3. Create `skills/sheets/src/claws_sheets/sheets.py` (per D-07, D-08, D-09, D-14, D-15):
       - Constants:
         AUTH_PORT = 8301
         SHEETS_SCOPE = "https://www.googleapis.com/auth/spreadsheets"
         DRIVE_READONLY_SCOPE = "https://www.googleapis.com/auth/drive.readonly"
         SHEETS_BASE = "https://sheets.googleapis.com/v4/spreadsheets"
         DRIVE_BASE = "https://www.googleapis.com/drive/v3"
       - `get_access_token(as_user=None)` -- requests BOTH scopes: [SHEETS_SCOPE, DRIVE_READONLY_SCOPE]
       - `_sheets_headers(token)` -- {"Authorization": f"Bearer {token}"}
       - `_sheets_get(url, token, params=None)` -- full URL (for flexibility between Sheets and Drive bases)
       - `_sheets_post(url, token, body)` -- POST returning JSON
       - `_sheets_put(url, token, body, params=None)` -- PUT returning JSON
       - `list_spreadsheets(max_results=100, as_user=None) -> list[dict]`:
         GET {DRIVE_BASE}/files with params q="mimeType='application/vnd.google-apps.spreadsheet'", pageSize, fields="files(id,name,modifiedTime)"
       - `read_values(spreadsheet_id, range_, as_user=None) -> list[list]`:
         GET {SHEETS_BASE}/{spreadsheet_id}/values/{range_}, return data.get("values", [])
       - `write_values(spreadsheet_id, range_, values, as_user=None) -> dict`:
         PUT {SHEETS_BASE}/{spreadsheet_id}/values/{range_} with params={"valueInputOption": "USER_ENTERED"}, body={"range": range_, "values": values}
       - `create_spreadsheet(title, as_user=None) -> dict`:
         POST {SHEETS_BASE} with body={"properties": {"title": title}}, return {"spreadsheetId": ..., "title": ...}
       - `handle_sheets_error(e)` -- same pattern as other skills

    4. Create `skills/sheets/tests/test_sheets.py`:
       - Fixtures: mock_auth_client, mock_httpx
       - Test get_access_token requests both scopes
       - Test list_spreadsheets uses Drive API with correct mimeType query
       - Test read_values calls correct Sheets API URL
       - Test write_values includes valueInputOption=USER_ENTERED
       - Test create_spreadsheet posts with properties.title
       - Test handle_sheets_error for 401, 403, 404, 429
       - Test as_user pass-through

    Write tests first (RED), then implement sheets.py (GREEN).
  </action>
  <verify>
    <automated>cd /Users/little-dank/code/lobster_claws && uv run pytest skills/sheets/tests/test_sheets.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - skills/sheets/src/claws_sheets/sheets.py contains `SHEETS_SCOPE = "https://www.googleapis.com/auth/spreadsheets"`
    - skills/sheets/src/claws_sheets/sheets.py contains `DRIVE_READONLY_SCOPE = "https://www.googleapis.com/auth/drive.readonly"`
    - skills/sheets/src/claws_sheets/sheets.py contains `SHEETS_BASE = "https://sheets.googleapis.com/v4/spreadsheets"`
    - skills/sheets/src/claws_sheets/sheets.py contains `def list_spreadsheets(`
    - skills/sheets/src/claws_sheets/sheets.py contains `def read_values(`
    - skills/sheets/src/claws_sheets/sheets.py contains `def write_values(`
    - skills/sheets/src/claws_sheets/sheets.py contains `def create_spreadsheet(`
    - skills/sheets/src/claws_sheets/sheets.py contains `valueInputOption`
    - skills/sheets/src/claws_sheets/sheets.py contains `application/vnd.google-apps.spreadsheet`
    - skills/sheets/tests/test_sheets.py exits 0
    - skills/sheets/pyproject.toml contains `name = "claws-sheets"`
  </acceptance_criteria>
  <done>All Sheets API functions implemented and tested. Dual scope for list, valueInputOption for write, and error handling all verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Sheets CLI with tests and register workspace</name>
  <files>
    skills/sheets/src/claws_sheets/cli.py,
    skills/sheets/tests/test_sheets_cli.py,
    pyproject.toml
  </files>
  <read_first>
    skills/gmail/src/claws_gmail/cli.py,
    skills/gmail/tests/test_gmail_cli.py,
    skills/sheets/src/claws_sheets/sheets.py,
    pyproject.toml
  </read_first>
  <behavior>
    - `claws sheets list` calls list_spreadsheets(max_results=100, as_user=None)
    - `claws sheets list --max 20` passes max_results=20
    - `claws sheets read SPREADSHEET_ID "Sheet1!A1:B5"` calls read_values(SPREADSHEET_ID, "Sheet1!A1:B5")
    - `claws sheets write SPREADSHEET_ID "Sheet1!A1:B2" --values '[["a","b"],["c","d"]]'` calls write_values with parsed JSON values
    - `claws sheets create --title "My Sheet"` calls create_spreadsheet(title="My Sheet")
    - `claws sheets --as user@x.com list` passes as_user through
    - HTTPStatusError routes to handle_sheets_error
    - ConnectionError and TimeoutError route to crash()
  </behavior>
  <action>
    1. Create `skills/sheets/src/claws_sheets/cli.py` (per D-07, D-08, D-13, D-15):
       ```python
       import json

       def main():
           parser = argparse.ArgumentParser(prog="claws-sheets", description="Google Sheets skill")
           parser.add_argument("--as", dest="as_user", help="Act as this Google Workspace user (email)")
           subs = parser.add_subparsers(dest="command", required=True)

           # list
           list_p = subs.add_parser("list", help="List spreadsheets")
           list_p.add_argument("--max", type=int, default=100, help="Max results (default: 100)")

           # read
           read_p = subs.add_parser("read", help="Read cell values")
           read_p.add_argument("spreadsheet_id", help="Spreadsheet ID")
           read_p.add_argument("range", help="A1 notation range (e.g., Sheet1!A1:B5)")

           # write
           write_p = subs.add_parser("write", help="Write cell values")
           write_p.add_argument("spreadsheet_id", help="Spreadsheet ID")
           write_p.add_argument("range", help="A1 notation range")
           write_p.add_argument("--values", required=True, help='JSON 2D array (e.g., \'[["a","b"],["c","d"]]\')')

           # create
           create_p = subs.add_parser("create", help="Create a new spreadsheet")
           create_p.add_argument("--title", required=True, help="Spreadsheet title")

           args = parser.parse_args()
           try:
               if args.command == "write":
                   values = json.loads(args.values)  # parse JSON string to list
                   # ... call write_values(args.spreadsheet_id, args.range, values, as_user=args.as_user)
               # ... other subcommands
           except json.JSONDecodeError as e:
               fail(f"Invalid JSON for --values: {e}")
           except httpx.HTTPStatusError as e:
               handle_sheets_error(e)
           except ConnectionError as e:
               crash(str(e))
           except TimeoutError as e:
               crash(str(e))
       ```

    2. Create `skills/sheets/tests/test_sheets_cli.py` following test_gmail_cli.py pattern.

    3. Update root `pyproject.toml`:
       - Add `"claws-sheets"` to `[dependency-groups] dev` list
       - Add `claws-sheets = { workspace = true }` to `[tool.uv.sources]`

    4. Run `uv sync`.

    Write tests first (RED), then implement cli.py (GREEN).
  </action>
  <verify>
    <automated>cd /Users/little-dank/code/lobster_claws && uv sync && uv run pytest skills/sheets -x -v</automated>
  </verify>
  <acceptance_criteria>
    - skills/sheets/src/claws_sheets/cli.py contains `def main():`
    - skills/sheets/src/claws_sheets/cli.py contains `prog="claws-sheets"`
    - skills/sheets/src/claws_sheets/cli.py contains `dest="as_user"`
    - skills/sheets/src/claws_sheets/cli.py contains `json.loads`
    - skills/sheets/src/claws_sheets/cli.py contains `handle_sheets_error`
    - skills/sheets/tests/test_sheets_cli.py exits 0
    - pyproject.toml contains `"claws-sheets"` in dev dependencies
    - pyproject.toml contains `claws-sheets = { workspace = true }` in uv.sources
    - `uv run claws sheets --help` exits 0
  </acceptance_criteria>
  <done>Sheets CLI routes all subcommands correctly. JSON parsing for --values works. Package registered in workspace and discoverable via `claws sheets`.</done>
</task>

</tasks>

<verification>
- `uv run pytest skills/sheets -x -v` -- all sheets tests pass
- `uv run claws sheets --help` -- shows subcommands (list, read, write, create)
- `uv run pytest` -- full suite passes (no regressions)
</verification>

<success_criteria>
- claws-sheets package exists with sheets.py (API client) and cli.py (CLI entry point)
- Data-only operations: list, read, write, create (no formatting per D-07)
- Dual scopes requested for Drive API access
- valueInputOption=USER_ENTERED included in write requests
- --as flag supported on all operations
- All tests pass, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/01-add-google-tasks-contacts-sheets-and-docs-skills/01-03-SUMMARY.md`
</output>
