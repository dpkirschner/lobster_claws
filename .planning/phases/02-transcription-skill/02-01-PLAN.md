---
phase: 02-transcription-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/whisper/pyproject.toml
  - servers/whisper/src/whisper_server/__init__.py
  - servers/whisper/src/whisper_server/app.py
  - servers/whisper/tests/__init__.py
  - servers/whisper/tests/test_app.py
autonomous: true
requirements: [WHSP-01, WHSP-02, WHSP-03, WHSP-04]

must_haves:
  truths:
    - "GET /health returns JSON with status, service name, and default model"
    - "POST /transcribe accepts a file upload and returns JSON with text field"
    - "POST /transcribe with model query param passes that model to mlx_whisper"
    - "Default model is preloaded at server startup via lifespan"
  artifacts:
    - path: "servers/whisper/pyproject.toml"
      provides: "Package definition with fastapi, uvicorn, mlx-whisper, python-multipart deps"
      contains: 'name = "whisper-server"'
    - path: "servers/whisper/src/whisper_server/app.py"
      provides: "FastAPI app with /transcribe, /health endpoints and lifespan preload"
      exports: ["app"]
      min_lines: 40
    - path: "servers/whisper/tests/test_app.py"
      provides: "Unit tests mocking mlx_whisper for all 4 WHSP requirements"
      min_lines: 50
  key_links:
    - from: "servers/whisper/src/whisper_server/app.py"
      to: "mlx_whisper.transcribe"
      via: "direct call with path_or_hf_repo param"
      pattern: "mlx_whisper\\.transcribe"
    - from: "servers/whisper/src/whisper_server/app.py"
      to: "FastAPI lifespan"
      via: "asynccontextmanager preloading model"
      pattern: "async def lifespan"
---

<objective>
Create the whisper-server FastAPI application with POST /transcribe (audio file upload), GET /health, model selection via query parameter, and model preloading at startup via lifespan.

Purpose: This is the core ML inference server that runs on the Mac mini host with Apple Silicon GPU access. It bridges HTTP to mlx-whisper's transcribe() API.
Output: Working whisper-server package with full test coverage (mocked mlx_whisper).
</objective>

<execution_context>
@/Users/little-dank/.claude/get-shit-done/workflows/execute-plan.md
@/Users/little-dank/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transcription-skill/02-RESEARCH.md
@.planning/phases/02-transcription-skill/02-VALIDATION.md

<interfaces>
<!-- From Phase 1 - package structure pattern to follow -->
From common/pyproject.toml:
```toml
[project]
name = "claws-common"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```

From pyproject.toml (root workspace):
```toml
[tool.uv.workspace]
members = ["common", "skills/*", "servers/*"]

[tool.pytest.ini_options]
testpaths = ["common/tests", "skills", "servers"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Whisper server package skeleton and test scaffolds</name>
  <files>
    servers/whisper/pyproject.toml,
    servers/whisper/src/whisper_server/__init__.py,
    servers/whisper/tests/__init__.py,
    servers/whisper/tests/test_app.py
  </files>
  <read_first>
    common/pyproject.toml,
    pyproject.toml
  </read_first>
  <behavior>
    - test_health: GET /health returns 200 with {"status": "ok", "service": "whisper-server", "default_model": "mlx-community/whisper-turbo"}
    - test_transcribe: POST /transcribe with a .wav file upload returns 200 with {"text": "..."} where text comes from mocked mlx_whisper.transcribe
    - test_transcribe_with_model: POST /transcribe?model=mlx-community/whisper-large-v3-mlx passes that model as path_or_hf_repo to mlx_whisper.transcribe
    - test_model_preload: lifespan event triggers model loading (mock verifies ModelHolder or transcribe called at startup)
    - test_transcribe_clears_cache: After transcription, mx.metal.clear_cache() is called (mock verifies; skip gracefully if mx.metal unavailable)
  </behavior>
  <action>
    1. Create servers/whisper/pyproject.toml:
       - name = "whisper-server", version = "0.1.0"
       - requires-python = ">=3.12"
       - dependencies = ["fastapi>=0.135", "uvicorn>=0.42", "mlx-whisper>=0.4.3", "python-multipart>=0.0.18"]
       - build-system: hatchling (same as common/pyproject.toml)
       - [project.scripts]: whisper-server = "whisper_server.app:main"

    2. Create servers/whisper/src/whisper_server/__init__.py (empty)

    3. Create servers/whisper/tests/__init__.py (empty)

    4. Create servers/whisper/tests/test_app.py with 5 tests:
       - Use `from fastapi.testclient import TestClient` (from httpx)
       - Mock `mlx_whisper.transcribe` to return {"text": " hello world"}
       - Mock `mlx_whisper.transcribe.ModelHolder` for preload test (try/except import for direct access vs dummy transcribe)
       - For cache test: mock `mx.metal.clear_cache` if available
       - Use `io.BytesIO` with a minimal WAV header for file upload tests
       - Import app via `from whisper_server.app import app` — tests will FAIL (RED) until Task 2

    5. Run `uv sync` to register the new workspace member

    6. Verify tests fail with ImportError (RED state): `uv run pytest servers/whisper/tests/test_app.py -x 2>&1 | head -20`
  </action>
  <verify>
    <automated>uv run pytest servers/whisper/tests/test_app.py -x 2>&1 | grep -E "(FAILED|ERROR|ImportError)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "whisper-server"' servers/whisper/pyproject.toml` exits 0
    - `grep -q 'mlx-whisper' servers/whisper/pyproject.toml` exits 0
    - `grep -q 'hatchling' servers/whisper/pyproject.toml` exits 0
    - `grep -q 'def test_health' servers/whisper/tests/test_app.py` exits 0
    - `grep -q 'def test_transcribe' servers/whisper/tests/test_app.py` exits 0
    - `grep -q 'def test_model_preload' servers/whisper/tests/test_app.py` exits 0
    - File servers/whisper/src/whisper_server/__init__.py exists
    - File servers/whisper/tests/__init__.py exists
    - `uv sync` exits 0 (workspace resolves with new member)
  </acceptance_criteria>
  <done>Package skeleton exists, uv sync succeeds, 5 tests exist and fail with ImportError (RED)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement whisper server app (GREEN)</name>
  <files>servers/whisper/src/whisper_server/app.py</files>
  <read_first>
    servers/whisper/tests/test_app.py,
    servers/whisper/pyproject.toml
  </read_first>
  <behavior>
    - All 5 tests from Task 1 pass (GREEN)
  </behavior>
  <action>
    Create servers/whisper/src/whisper_server/app.py with:

    1. Constants:
       - DEFAULT_MODEL = "mlx-community/whisper-turbo"
       - DEFAULT_PORT = 8300

    2. Lifespan context manager (WHSP-04):
       - Try to import `from mlx_whisper.transcribe import ModelHolder` and call `ModelHolder.get_model(DEFAULT_MODEL)`
       - If that import fails, fall back to calling `mlx_whisper.transcribe("", path_or_hf_repo=DEFAULT_MODEL)` wrapped in try/except to trigger model download
       - Log to stderr: "Preloading model: {DEFAULT_MODEL}"

    3. FastAPI app:
       - `app = FastAPI(title="whisper-server", version="0.1.0", lifespan=lifespan)`

    4. GET /health endpoint (WHSP-02):
       - Returns `{"status": "ok", "service": "whisper-server", "default_model": DEFAULT_MODEL}`

    5. POST /transcribe endpoint (WHSP-01, WHSP-03):
       - Parameters: `file: UploadFile`, `model: str = Query(default=None)`
       - Determine model_id = model or DEFAULT_MODEL
       - Get suffix from file.filename (default ".wav")
       - Write to tempfile.NamedTemporaryFile(suffix=suffix, delete=True)
       - Call `mlx_whisper.transcribe(tmp.name, path_or_hf_repo=model_id)`
       - Try `mx.metal.clear_cache()` after transcription (import mx at top; wrap in try/except AttributeError for environments where it is unavailable)
       - Return `{"text": result["text"]}`

    6. main() function for script entry:
       ```python
       def main():
           import uvicorn
           uvicorn.run(app, host="0.0.0.0", port=DEFAULT_PORT)
       ```

    7. Bind to 0.0.0.0 (NOT 127.0.0.1) so Docker containers can reach via host.docker.internal

    Run tests: `uv run pytest servers/whisper/tests/test_app.py -x -v`
  </action>
  <verify>
    <automated>uv run pytest servers/whisper/tests/test_app.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'DEFAULT_MODEL = "mlx-community/whisper-turbo"' servers/whisper/src/whisper_server/app.py` exits 0
    - `grep -q 'async def lifespan' servers/whisper/src/whisper_server/app.py` exits 0
    - `grep -q '0.0.0.0' servers/whisper/src/whisper_server/app.py` exits 0
    - `grep -q 'async def health' servers/whisper/src/whisper_server/app.py` exits 0
    - `grep -q 'async def transcribe' servers/whisper/src/whisper_server/app.py` exits 0
    - `grep -q 'clear_cache' servers/whisper/src/whisper_server/app.py` exits 0
    - `grep -q 'NamedTemporaryFile' servers/whisper/src/whisper_server/app.py` exits 0
    - `uv run pytest servers/whisper/tests/test_app.py -x` exits 0 (all tests GREEN)
  </acceptance_criteria>
  <done>All 5 whisper server tests pass. Server has /health, /transcribe, model selection, lifespan preload, and cache clearing.</done>
</task>

</tasks>

<verification>
- `uv run pytest servers/whisper/tests/ -x -v` passes all tests
- `uv run ruff check servers/whisper/` passes lint
- `grep -q "whisper-server" servers/whisper/pyproject.toml` confirms package name
</verification>

<success_criteria>
- whisper-server package is a valid uv workspace member (uv sync succeeds)
- FastAPI app has /health and /transcribe endpoints
- Model selection via query parameter works (tested with mock)
- Model preloading at startup via lifespan works (tested with mock)
- All tests pass with mocked mlx_whisper (no GPU required for tests)
</success_criteria>

<output>
After completion, create `.planning/phases/02-transcription-skill/02-01-SUMMARY.md`
</output>
