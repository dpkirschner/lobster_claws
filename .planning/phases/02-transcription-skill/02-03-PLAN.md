---
phase: 02-transcription-skill
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - launchd/com.lobsterclaws.whisper.plist
  - tests/test_launchd.py
  - pyproject.toml
autonomous: true
requirements: [INFR-01]

must_haves:
  truths:
    - "launchd plist is valid XML with correct structure"
    - "Plist has RunAtLoad and KeepAlive set to true for auto-start and crash recovery"
    - "ProgramArguments uses absolute path to uvicorn and correct module path"
    - "Server binds to 0.0.0.0:8300"
    - "EnvironmentVariables includes PATH with /opt/homebrew/bin and WHISPER_MODEL"
    - "All paths in plist are absolute (no ~ expansion)"
  artifacts:
    - path: "launchd/com.lobsterclaws.whisper.plist"
      provides: "launchd user agent for auto-starting whisper server"
      contains: "com.lobsterclaws.whisper"
    - path: "tests/test_launchd.py"
      provides: "Plist validation tests parsing XML and checking required keys"
      min_lines: 30
  key_links:
    - from: "launchd/com.lobsterclaws.whisper.plist"
      to: "whisper_server.app:app"
      via: "ProgramArguments calling uvicorn with module path"
      pattern: "whisper_server.app:app"
---

<objective>
Create the launchd plist for auto-starting the whisper server on Mac mini reboot with crash recovery, plus a validation test and root testpaths update.

Purpose: INFR-01 requires the whisper server to auto-start after reboot and auto-restart after crash. launchd is the macOS-native process supervisor.
Output: Valid launchd plist and plist validation test.
</objective>

<execution_context>
@/Users/little-dank/.claude/get-shit-done/workflows/execute-plan.md
@/Users/little-dank/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transcription-skill/02-RESEARCH.md
@.planning/phases/02-transcription-skill/02-VALIDATION.md
@.planning/phases/02-transcription-skill/02-01-SUMMARY.md

<interfaces>
<!-- Whisper server entry point from Plan 01 -->
Server module: whisper_server.app:app
Default port: 8300
Server working directory: servers/whisper/

<!-- Root pyproject.toml testpaths (needs update) -->
From pyproject.toml:
```toml
[tool.pytest.ini_options]
testpaths = ["common/tests", "skills", "servers"]
```
Note: "tests/" (root-level) is NOT currently in testpaths. Must add it for test_launchd.py discovery.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: launchd plist and validation test</name>
  <files>
    launchd/com.lobsterclaws.whisper.plist,
    tests/test_launchd.py,
    pyproject.toml
  </files>
  <read_first>
    pyproject.toml,
    servers/whisper/src/whisper_server/app.py
  </read_first>
  <behavior>
    - test_plist_valid_xml: plist file parses as valid XML without errors
    - test_plist_label: Label is "com.lobsterclaws.whisper"
    - test_plist_run_at_load: RunAtLoad is true
    - test_plist_keep_alive: KeepAlive is true
    - test_plist_program_args: ProgramArguments contains "whisper_server.app:app", "--host", "0.0.0.0", "--port", "8300"
    - test_plist_no_tilde: No path in plist contains "~"
    - test_plist_env_path: EnvironmentVariables PATH includes "/opt/homebrew/bin"
    - test_plist_log_paths: StandardOutPath and StandardErrorPath are absolute paths under /Users/little-dank/Library/Logs/lobsterclaws/
  </behavior>
  <action>
    1. Create launchd/com.lobsterclaws.whisper.plist:
       ```xml
       <?xml version="1.0" encoding="UTF-8"?>
       <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
         "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
       <plist version="1.0">
       <dict>
           <key>Label</key>
           <string>com.lobsterclaws.whisper</string>

           <key>ProgramArguments</key>
           <array>
               <string>/Users/little-dank/code/lobster_claws/.venv/bin/uvicorn</string>
               <string>whisper_server.app:app</string>
               <string>--host</string>
               <string>0.0.0.0</string>
               <string>--port</string>
               <string>8300</string>
           </array>

           <key>RunAtLoad</key>
           <true/>

           <key>KeepAlive</key>
           <true/>

           <key>WorkingDirectory</key>
           <string>/Users/little-dank/code/lobster_claws/servers/whisper</string>

           <key>EnvironmentVariables</key>
           <dict>
               <key>PATH</key>
               <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
               <key>WHISPER_MODEL</key>
               <string>mlx-community/whisper-turbo</string>
           </dict>

           <key>StandardOutPath</key>
           <string>/Users/little-dank/Library/Logs/lobsterclaws/whisper.log</string>

           <key>StandardErrorPath</key>
           <string>/Users/little-dank/Library/Logs/lobsterclaws/whisper.err</string>
       </dict>
       </plist>
       ```

    2. Create tests/test_launchd.py:
       - Use `plistlib.load()` to parse the plist (stdlib, no deps)
       - Load from `Path(__file__).parent.parent / "launchd" / "com.lobsterclaws.whisper.plist"`
       - 8 test functions checking all behavior items above
       - For tilde test: read raw file text and assert "~" not in it

    3. Update pyproject.toml testpaths:
       - Change `testpaths = ["common/tests", "skills", "servers"]`
       - To: `testpaths = ["common/tests", "skills", "servers", "tests"]`

    4. Run full suite to verify nothing broke: `uv run pytest -x -v`
  </action>
  <verify>
    <automated>uv run pytest tests/test_launchd.py -x -v && uv run pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import plistlib; plistlib.load(open('launchd/com.lobsterclaws.whisper.plist', 'rb'))"` exits 0
    - `grep -q 'com.lobsterclaws.whisper' launchd/com.lobsterclaws.whisper.plist` exits 0
    - `grep -q 'RunAtLoad' launchd/com.lobsterclaws.whisper.plist` exits 0
    - `grep -q 'KeepAlive' launchd/com.lobsterclaws.whisper.plist` exits 0
    - `grep -q '0.0.0.0' launchd/com.lobsterclaws.whisper.plist` exits 0
    - `grep -q '8300' launchd/com.lobsterclaws.whisper.plist` exits 0
    - `grep -q '/opt/homebrew/bin' launchd/com.lobsterclaws.whisper.plist` exits 0
    - Plist file does not contain tilde character
    - `grep -q '"tests"' pyproject.toml` exits 0 (testpaths updated)
    - `uv run pytest tests/test_launchd.py -x` exits 0 (all plist tests pass)
    - `uv run pytest -x` exits 0 (full suite passes including Phase 1 + Phase 2 tests)
  </acceptance_criteria>
  <done>Valid launchd plist exists, all 8 validation tests pass, root testpaths includes tests/, full test suite green.</done>
</task>

</tasks>

<verification>
- `uv run pytest -x -v` passes all tests (Phase 1 + Phase 2)
- `uv run ruff check .` passes lint
- `python3 -c "import plistlib; plistlib.load(open('launchd/com.lobsterclaws.whisper.plist', 'rb'))"` validates plist
</verification>

<success_criteria>
- launchd plist is valid XML parseable by plistlib
- Plist has RunAtLoad=true and KeepAlive=true
- All paths in plist are absolute (no ~ usage)
- Server binds to 0.0.0.0:8300 via ProgramArguments
- PATH in EnvironmentVariables includes /opt/homebrew/bin
- Root testpaths updated to include tests/ for plist validation tests
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-transcription-skill/02-03-SUMMARY.md`
</output>
