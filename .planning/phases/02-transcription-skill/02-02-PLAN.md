---
phase: 02-transcription-skill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/transcribe/pyproject.toml
  - skills/transcribe/src/claws_transcribe/__init__.py
  - skills/transcribe/src/claws_transcribe/cli.py
  - skills/transcribe/tests/__init__.py
  - skills/transcribe/tests/test_cli.py
autonomous: true
requirements: [TRNS-01, TRNS-02, TRNS-03, TRNS-04]

must_haves:
  truths:
    - "CLI accepts an audio file path and prints transcription text to stdout"
    - "CLI --format json prints JSON object to stdout instead of plain text"
    - "CLI --model flag passes model name to whisper server as query parameter"
    - "CLI output uses flush=True via claws_common.output (PYTHONUNBUFFERED compatible)"
    - "File-not-found produces stderr error and exit code 1"
    - "Connection failure produces stderr error and exit code 2"
  artifacts:
    - path: "skills/transcribe/pyproject.toml"
      provides: "Package definition with claws-common dependency and claws-transcribe entry point"
      contains: 'name = "claws-transcribe"'
    - path: "skills/transcribe/src/claws_transcribe/cli.py"
      provides: "CLI entry point using ClawsClient and structured output"
      exports: ["main"]
      min_lines: 30
    - path: "skills/transcribe/tests/test_cli.py"
      provides: "Unit tests mocking ClawsClient for all 4 TRNS requirements"
      min_lines: 40
  key_links:
    - from: "skills/transcribe/src/claws_transcribe/cli.py"
      to: "claws_common.client.ClawsClient"
      via: "import and instantiate with service='whisper', port=8300, timeout=300.0"
      pattern: "ClawsClient.*service.*whisper.*port.*8300"
    - from: "skills/transcribe/src/claws_transcribe/cli.py"
      to: "claws_common.output"
      via: "import result, fail, crash"
      pattern: "from claws_common\\.output import"
---

<objective>
Create the claws-transcribe CLI skill that accepts an audio file path, POSTs it to the whisper server via ClawsClient, and prints transcription text to stdout with support for --model and --format flags.

Purpose: This is the container-side thin CLI that the AI agent invokes. It follows the claw pattern: argparse + ClawsClient + structured output. No business logic lives here.
Output: Working claws-transcribe package with full test coverage (mocked ClawsClient).
</objective>

<execution_context>
@/Users/little-dank/.claude/get-shit-done/workflows/execute-plan.md
@/Users/little-dank/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transcription-skill/02-RESEARCH.md
@.planning/phases/02-transcription-skill/02-VALIDATION.md

<interfaces>
<!-- ClawsClient API from Phase 1 -->
From common/src/claws_common/client.py:
```python
class ClawsClient:
    def __init__(self, service: str, port: int, timeout: float = 30.0):
    def get(self, path: str) -> dict:
    def post_file(self, path: str, file_path: str, **params) -> dict:
```

From common/src/claws_common/output.py:
```python
def result(data: str | dict) -> None:  # stdout, flush=True
def fail(message: str) -> None:        # stderr, exit code 1
def crash(message: str) -> None:       # stderr, exit code 2
```

From common/pyproject.toml (package pattern):
```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transcribe CLI package skeleton and test scaffolds</name>
  <files>
    skills/transcribe/pyproject.toml,
    skills/transcribe/src/claws_transcribe/__init__.py,
    skills/transcribe/tests/__init__.py,
    skills/transcribe/tests/test_cli.py
  </files>
  <read_first>
    common/pyproject.toml,
    common/src/claws_common/client.py,
    common/src/claws_common/output.py,
    pyproject.toml
  </read_first>
  <behavior>
    - test_transcribe_success: main() with valid file path calls ClawsClient.post_file("/transcribe", file_path) and prints response["text"] to stdout via result()
    - test_transcribe_json_format: main() with --format json calls result() with the full response dict (JSON output)
    - test_transcribe_text_format: main() with --format text (or default) calls result() with response["text"] (plain text output)
    - test_model_flag: main() with --model mlx-community/whisper-large-v3-mlx passes model="mlx-community/whisper-large-v3-mlx" as kwarg to post_file
    - test_file_not_found: main() with nonexistent path calls fail() with "File not found" message
    - test_connection_error: ClawsClient.post_file raises ConnectionError -> main() calls crash()
    - test_timeout_error: ClawsClient.post_file raises TimeoutError -> main() calls crash()
  </behavior>
  <action>
    1. Create skills/transcribe/pyproject.toml:
       - name = "claws-transcribe", version = "0.1.0"
       - description = "Audio transcription skill for Lobster Claws"
       - requires-python = ">=3.12"
       - dependencies = ["claws-common"]
       - build-system: hatchling
       - [project.scripts]: claws-transcribe = "claws_transcribe.cli:main"
       - [tool.uv.sources]: claws-common = { workspace = true }

    2. Create skills/transcribe/src/claws_transcribe/__init__.py (empty)

    3. Create skills/transcribe/tests/__init__.py (empty)

    4. Create skills/transcribe/tests/test_cli.py with 7 tests:
       - Use `unittest.mock.patch` to mock ClawsClient and output functions
       - Use `tmp_path` fixture to create a real temporary audio file for "exists" tests
       - Mock sys.argv for argument parsing: `["claws-transcribe", str(tmp_file)]`
       - For file-not-found: use a path that does not exist
       - Mock `claws_common.client.ClawsClient` so no real HTTP calls
       - Mock `claws_common.output.result`, `claws_common.output.fail`, `claws_common.output.crash` to capture calls (use side_effect=SystemExit for fail/crash since they call sys.exit)
       - Import cli.main -> tests will FAIL (RED) until Task 2

    5. Run `uv sync` to register the new workspace member

    6. Verify tests fail (RED): `uv run pytest skills/transcribe/tests/test_cli.py -x 2>&1 | head -20`
  </action>
  <verify>
    <automated>uv run pytest skills/transcribe/tests/test_cli.py -x 2>&1 | grep -E "(FAILED|ERROR|ImportError)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "claws-transcribe"' skills/transcribe/pyproject.toml` exits 0
    - `grep -q 'claws-common' skills/transcribe/pyproject.toml` exits 0
    - `grep -q 'claws-transcribe = "claws_transcribe.cli:main"' skills/transcribe/pyproject.toml` exits 0
    - `grep -q 'workspace = true' skills/transcribe/pyproject.toml` exits 0
    - `grep -q 'def test_transcribe_success' skills/transcribe/tests/test_cli.py` exits 0
    - `grep -q 'def test_model_flag' skills/transcribe/tests/test_cli.py` exits 0
    - `grep -q 'def test_file_not_found' skills/transcribe/tests/test_cli.py` exits 0
    - File skills/transcribe/src/claws_transcribe/__init__.py exists
    - `uv sync` exits 0
  </acceptance_criteria>
  <done>Package skeleton exists, uv sync succeeds, 7 tests exist and fail with ImportError (RED)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transcribe CLI (GREEN)</name>
  <files>skills/transcribe/src/claws_transcribe/cli.py</files>
  <read_first>
    skills/transcribe/tests/test_cli.py,
    skills/transcribe/pyproject.toml,
    common/src/claws_common/client.py,
    common/src/claws_common/output.py
  </read_first>
  <behavior>
    - All 7 tests from Task 1 pass (GREEN)
  </behavior>
  <action>
    Create skills/transcribe/src/claws_transcribe/cli.py with:

    1. Imports:
       ```python
       import argparse
       from pathlib import Path
       from claws_common.client import ClawsClient
       from claws_common.output import result, fail, crash
       ```

    2. main() function:
       - Create argparse.ArgumentParser with prog="claws-transcribe", description="Transcribe audio files via whisper server"
       - Add positional arg "file" (help="Path to audio file")
       - Add optional --model (help="Whisper model to use (e.g. mlx-community/whisper-large-v3-mlx)")
       - Add optional --format with choices=["text", "json"], default="text" (help="Output format (default: text)")
       - Parse args
       - Check Path(args.file).exists() -> if not, call fail(f"File not found: {args.file}")
       - Create ClawsClient(service="whisper", port=8300, timeout=300.0)
       - Build params dict: if args.model, set params["model"] = args.model
       - try: response = client.post_file("/transcribe", str(Path(args.file)), **params)
       - except ConnectionError as e: crash(str(e))
       - except TimeoutError as e: crash(str(e))
       - if args.format == "json": result(response)
       - else: result(response["text"])

    3. if __name__ == "__main__": main()

    Run tests: `uv run pytest skills/transcribe/tests/test_cli.py -x -v`
  </action>
  <verify>
    <automated>uv run pytest skills/transcribe/tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'ClawsClient(service="whisper", port=8300, timeout=300.0)' skills/transcribe/src/claws_transcribe/cli.py` exits 0
    - `grep -q 'from claws_common.client import ClawsClient' skills/transcribe/src/claws_transcribe/cli.py` exits 0
    - `grep -q 'from claws_common.output import result, fail, crash' skills/transcribe/src/claws_transcribe/cli.py` exits 0
    - `grep -q 'post_file("/transcribe"' skills/transcribe/src/claws_transcribe/cli.py` exits 0
    - `grep -q '\-\-model' skills/transcribe/src/claws_transcribe/cli.py` exits 0
    - `grep -q '\-\-format' skills/transcribe/src/claws_transcribe/cli.py` exits 0
    - `uv run pytest skills/transcribe/tests/test_cli.py -x` exits 0 (all tests GREEN)
  </acceptance_criteria>
  <done>All 7 CLI tests pass. CLI has file argument, --model flag, --format flag, proper error handling with fail/crash.</done>
</task>

</tasks>

<verification>
- `uv run pytest skills/transcribe/tests/ -x -v` passes all tests
- `uv run ruff check skills/transcribe/` passes lint
- `grep -q "claws-transcribe" skills/transcribe/pyproject.toml` confirms package name
</verification>

<success_criteria>
- claws-transcribe package is a valid uv workspace member (uv sync succeeds)
- CLI parses file, --model, and --format arguments correctly
- ClawsClient called with service="whisper", port=8300, timeout=300.0
- Output uses result() for success, fail() for user errors, crash() for infra errors
- All tests pass with mocked ClawsClient (no server required)
</success_criteria>

<output>
After completion, create `.planning/phases/02-transcription-skill/02-02-SUMMARY.md`
</output>
