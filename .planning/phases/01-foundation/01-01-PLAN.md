---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - common/pyproject.toml
  - common/src/claws_common/__init__.py
  - skills/.gitkeep
  - servers/.gitkeep
autonomous: true
requirements: [INFR-02, INFR-03]

must_haves:
  truths:
    - "`uv sync` in the monorepo root resolves all workspace members without error"
    - "`.gitignore` covers standard Python artifacts, virtual environments, IDE files, and OS files"
    - "`claws-common` package directory exists with valid pyproject.toml using hatchling build backend"
  artifacts:
    - path: "pyproject.toml"
      provides: "Root workspace config with uv workspace members, dev deps, ruff config, pytest config"
      contains: "[tool.uv.workspace]"
    - path: ".gitignore"
      provides: "Standard Python monorepo gitignore"
      contains: "__pycache__/"
    - path: "common/pyproject.toml"
      provides: "claws-common package definition with hatchling backend"
      contains: "hatchling"
    - path: "common/src/claws_common/__init__.py"
      provides: "Package marker (placeholder until Plan 02 adds exports)"
  key_links:
    - from: "pyproject.toml"
      to: "common/pyproject.toml"
      via: "uv workspace members"
      pattern: 'members = \["common"'
---

<objective>
Create the monorepo scaffolding: root workspace config, .gitignore, and claws-common package skeleton.

Purpose: Every subsequent plan and phase depends on this workspace structure existing. The root pyproject.toml defines the uv workspace, dev dependencies (pytest, ruff), and shared tool config. The common package skeleton provides the target directory for library code.

Output: A working uv workspace that resolves with `uv sync`, a comprehensive .gitignore, and the claws-common package skeleton ready for library modules.
</objective>

<execution_context>
@/Users/little-dank/.claude/get-shit-done/workflows/execute-plan.md
@/Users/little-dank/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@data.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo scaffolding and root workspace config</name>
  <files>pyproject.toml, .gitignore, common/pyproject.toml, common/src/claws_common/__init__.py, skills/.gitkeep, servers/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: Root pyproject.toml, Pattern 2: Package pyproject.toml, .gitignore content)
    - data.md (container environment constraints, pip install patterns)
  </read_first>
  <action>
Create the following files with exact contents:

**1. `pyproject.toml` (root workspace config):**
```toml
[project]
name = "lobster-claws"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["common", "skills/*", "servers/*"]

[dependency-groups]
dev = ["ruff>=0.15", "pytest>=9.0", "pytest-httpx>=0.36"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
testpaths = ["common/tests", "skills", "servers"]
```

**2. `.gitignore`:**
```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# uv
.python-version

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# Environment
.env
.env.local
```

**3. `common/pyproject.toml`:**
```toml
[project]
name = "claws-common"
version = "0.1.0"
description = "Shared client library for Lobster Claws skills"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```

**4. `common/src/claws_common/__init__.py`:**
```python
"""Shared client library for Lobster Claws skills."""
```
(Placeholder -- Plan 02 will add public API exports after modules exist.)

**5. `skills/.gitkeep`** and **`servers/.gitkeep`:** Empty files to preserve directory structure.

**6. Create `common/tests/` directory** with an empty `__init__.py` so pytest can discover tests.

After creating all files, run `uv sync` to generate `uv.lock` and verify the workspace resolves.
  </action>
  <verify>
    <automated>cd /Users/little-dank/code/lobster_claws && uv sync 2>&1 | tail -5 && echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `[tool.uv.workspace]` with `members = ["common", "skills/*", "servers/*"]`
    - pyproject.toml contains `[dependency-groups]` with `dev = ["ruff>=0.15", "pytest>=9.0", "pytest-httpx>=0.36"]`
    - pyproject.toml contains `[tool.ruff]` with `target-version = "py312"` and `line-length = 100`
    - pyproject.toml contains `[tool.pytest.ini_options]` with `testpaths`
    - .gitignore contains `__pycache__/`, `*.egg-info/`, `.venv/`, `.DS_Store`, `.env`
    - common/pyproject.toml contains `name = "claws-common"` and `build-backend = "hatchling.build"`
    - common/pyproject.toml contains `dependencies = ["httpx>=0.28"]`
    - common/src/claws_common/__init__.py exists
    - `uv sync` exits with code 0
    - uv.lock file is generated
  </acceptance_criteria>
  <done>
    uv workspace resolves successfully with `uv sync`. Root pyproject.toml has workspace config, dev deps, ruff config, pytest config. claws-common package skeleton exists with hatchling build backend. .gitignore covers Python artifacts. skills/ and servers/ placeholder dirs exist.
  </done>
</task>

</tasks>

<verification>
- `uv sync` exits 0 (workspace resolves)
- `uv run pytest --co 2>&1` exits without import errors (pytest config valid)
- `.gitignore` exists with standard Python patterns
- `common/pyproject.toml` uses hatchling build backend
</verification>

<success_criteria>
- Working uv workspace: `uv sync` succeeds
- All six files/directories exist with correct contents
- uv.lock generated and present
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
